com/cb/entity/dataBaseEntity_jpa 这个包早期是建数据库所创建的实体类，用于一些简单表的自动创建，但现在由于有数据库文件，表自动创建功能用不上了
com/cb/entity/dataEntyty_mybatis 这个包是用mybatis-plus插件根据建好的表自动生成的
这两个包里的实体类都是数据库表的映射类，用于在代码中操作数据库表，你用谁都行，不用自己创建都可以