<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cb.mapper.LoginMapper">

    <select id="login" resultType="com.cb.entity.dataBaseEntity.UserEntity">
        SELECT id, authority, create_time, dept_id, person_name, phone_number, status, user_name
        FROM sys_user
        WHERE user_name = #{userName} AND user_password = #{password}
    </select>
</mapper>